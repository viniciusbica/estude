console.log(19,9 * 0,6);

let preco = 19.90;
var desconto = 0.4;
// A VARIAVEL  pode ser usada tanto com var quanto com let.

console.log(preco *(1 - desconto));
// para tornar prioridade o desconto coloquei ele dentro de um parentese.

// sempre escolher bons nomes para as variaveis e manter o codigo organizado!!